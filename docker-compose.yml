version: "3.8"
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:./data.db
    volumes:
      - mofe-data:/app/data
      - mofe-uploads:/app/public/uploads

volumes:
  mofe-data:
  mofe-uploads:
